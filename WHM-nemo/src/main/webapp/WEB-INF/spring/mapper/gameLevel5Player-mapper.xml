<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.whm0304.nemo.persistance.GameLevel5PlayerDao">
	<select id="selectAll">
		SELECT *
		FROM tbl_nemo_play
		WHERE p_id = 1 AND p_num = 5
	</select>

</mapper>